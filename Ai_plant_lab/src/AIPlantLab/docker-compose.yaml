version: '3.8'

services:
  postgres-ai:
    image: ankane/pgvector:latest
    container_name: ai_analysis_db
    environment:
      POSTGRES_USER: ai_user
      POSTGRES_PASSWORD: ai_password
      POSTGRES_DB: aianalysis_db
    ports:
      - "5432:5432"
    volumes:
      - ai_pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  postgres-experiments:
    image: postgres:16-alpine
    container_name: experiments_db
    environment:
      POSTGRES_USER: exp_user
      POSTGRES_PASSWORD: exp_password
      POSTGRES_DB: experiments_db
    ports:
      - "5433:5432"
    volumes:
      - experiments_pgdata:/var/lib/postgresql/data
    restart: unless-stopped

  rabbitmq:
    image: rabbitmq:3-management-alpine
    container_name: ai_rabbitmq
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    restart: unless-stopped

volumes:
  ai_pgdata:
  experiments_pgdata:
  rabbitmq_data: